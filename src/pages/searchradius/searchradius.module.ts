import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SearchradiusPage } from './searchradius';

@NgModule({
  declarations: [
    SearchradiusPage,
  ],
  imports: [
    IonicPageModule.forChild(SearchradiusPage),
  ],
})
export class SearchradiusPageModule {}
